///The way in which pressing the buttons on the controller is interpreted.
///
///PRESS - The command is executed only on press the button.
///
///HOLD - The command is executed while the button is pressed.
enum ButtonMode { PRESS, HOLD }
